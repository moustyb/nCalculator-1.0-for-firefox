// background.js - minimal MV3 service worker stub
// Keep this file intentionally minimal. No remote code, no data collection.
self.addEventListener('install', () => { /* no-op */ });
self.addEventListener('activate', () => { /* no-op */ });
